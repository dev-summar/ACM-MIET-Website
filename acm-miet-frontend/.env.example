# ===========================================
# ACM MIET Frontend - Environment Variables
# Copy to .env. VITE_* are exposed to the client at build time.
# ===========================================

# Backend API base URL (required in production)
# Must include /api path. No trailing slash.

# Local development (backend on port 5000):
VITE_API_URL=http://localhost:5000/api

# Production - set before building (e.g. npm run build):
# VITE_API_URL=https://api.your-domain.com/api
