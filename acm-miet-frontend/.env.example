# ===========================================
# ACM MIET Frontend - Environment Variables
# Copy to .env. VITE_* are exposed to the client at build time.
# All API calls use these - no hardcoded URLs. Production ready.
# ===========================================

# Backend API base URL (required for dev and production)
# Must include /api path. No trailing slash.
VITE_API_BASE_URL=http://localhost:5000/api

# Optional: Default hero video URL (fallback when API returns none)
# Used in admin Hero Settings. Set in .env.production if needed.
# VITE_DEFAULT_HERO_VIDEO_URL=

# Production - copy to .env.production before npm run build:
# VITE_API_BASE_URL=https://api.your-domain.com/api
