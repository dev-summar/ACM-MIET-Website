# Production - copy to .env.production before npm run build.
# All API calls use VITE_API_BASE_URL - no hardcoded URLs.
# Do NOT use localhost here.

VITE_API_BASE_URL=https://api.your-domain.com/api

# Optional: Default hero video URL (Cloudinary or other) when no settings in DB
# VITE_DEFAULT_HERO_VIDEO_URL=https://res.cloudinary.com/xxx/video/upload/xxx.mp4
