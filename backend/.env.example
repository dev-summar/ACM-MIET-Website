# ===========================================
# ACM MIET Backend - Environment Variables
# Copy to .env and fill in values. All config from env - production ready.
# ===========================================

# Node environment: development | production
NODE_ENV=development

# Server port (default 5000)
PORT=5000

# MongoDB connection string (required)
MONGODB_URI=mongodb+srv://user:password@cluster.mongodb.net/dbname?retryWrites=true&w=majority

# JWT secret for admin auth (required - use a long random string in production)
JWT_SECRET=your_jwt_secret_here

# Cloudinary (required for image uploads)
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret

# CORS - allowed frontend origin(s). Production: required.
# Single frontend URL (no trailing slash)
FRONTEND_URL=https://your-frontend-domain.com

# Optional: comma-separated extra CORS origins (e.g. multiple domains or preview URLs)
# CORS_ORIGINS=https://www.acmmiet.org,https://acmmiet.vercel.app

# Optional: dev CORS origins (defaults to localhost:5173,3000). Only used when NODE_ENV!=production
# DEV_CORS_ORIGINS=http://localhost:5173,http://localhost:3000,http://127.0.0.1:5173,http://127.0.0.1:3000

# Optional: Footer defaults (used when creating first footer document)
# DEFAULT_FOOTER_DESCRIPTION=Empowering students through technology...
# DEFAULT_CONTACT_EMAIL=acm.sb@mietjammu.in
# DEFAULT_CONTACT_ADDRESS=MIET Jammu Campus, Kot Bhalwal Jammu J&K
# DEFAULT_LINKEDIN_URL=https://in.linkedin.com/company/acmsc-miet
# DEFAULT_INSTAGRAM_URL=https://www.instagram.com/acmmiet/
# DEFAULT_FACEBOOK_URL=
# DEFAULT_TWITTER_URL=
